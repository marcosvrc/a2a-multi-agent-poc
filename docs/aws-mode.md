# Modo AWS (opcional)

**Ainda não implementado nesta milestone.** O agente
`aws-enrichment-agent` (Strands) será adicionado na Fase 7, conforme
`a2a-multi-agent-poc-PROJECT_SPEC.md` §5.6, §19-20 e §38.

Quando implementado, o sistema principal continuará funcional sem
credenciais AWS (`AWS_AGENT_ENABLED=false` por padrão).
